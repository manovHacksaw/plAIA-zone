
  {
  "scripts": {
    "test": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy": "npx hardhat run scripts/deploy.js --network aiaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.14"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
